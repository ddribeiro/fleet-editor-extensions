name: Build Linux Binaries

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version override (e.g., 0.1.2)"
        required: false
        type: string
      upload_to_release:
        description: "Upload to existing release tag (e.g., v0.1.2)"
        required: false
        type: string

env:
  RUST_BACKTRACE: 1
  BINARY_NAME: flint
  CARGO_INCREMENTAL: 0
  CARGO_TERM_COLOR: always

jobs:
  build-linux-x64:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-x64-cargo-${{ hashFiles('Cargo.lock') }}

      - name: Get version
        id: version
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION=$(grep '^version' Cargo.toml | head -n1 | cut -d'"' -f2)
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Building version: $VERSION"

      - name: Run tests
        run: cargo test --workspace --release

      - name: Build binary
        run: |
          cargo build --release -p ${{ env.BINARY_NAME }}
          mkdir -p dist
          cp target/release/${{ env.BINARY_NAME }} dist/
          strip dist/${{ env.BINARY_NAME }}

      - name: Test binary
        run: ./dist/${{ env.BINARY_NAME }} --version

      - name: Create distribution archive
        working-directory: dist
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          ARCHIVE="${{ env.BINARY_NAME }}-${VERSION}-linux-x64.tar.gz"

          sha256sum ${{ env.BINARY_NAME }} > ${{ env.BINARY_NAME }}.sha256
          tar -czf "$ARCHIVE" ${{ env.BINARY_NAME }}
          sha256sum "$ARCHIVE" > "${ARCHIVE}.sha256"

          echo "=== Distribution ==="
          ls -la

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.BINARY_NAME }}-linux-x64
          path: |
            dist/*.tar.gz
            dist/*.tar.gz.sha256
          retention-days: 90

  build-linux-arm64:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-unknown-linux-gnu

      - name: Install cross-compilation tools
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-arm64-cargo-${{ hashFiles('Cargo.lock') }}

      - name: Get version
        id: version
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION=$(grep '^version' Cargo.toml | head -n1 | cut -d'"' -f2)
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Building version: $VERSION"

      - name: Build binary
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc
        run: |
          cargo build --release -p ${{ env.BINARY_NAME }} --target aarch64-unknown-linux-gnu
          mkdir -p dist
          cp target/aarch64-unknown-linux-gnu/release/${{ env.BINARY_NAME }} dist/
          aarch64-linux-gnu-strip dist/${{ env.BINARY_NAME }} || echo "Strip failed, continuing..."

      - name: Create distribution archive
        working-directory: dist
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          ARCHIVE="${{ env.BINARY_NAME }}-${VERSION}-linux-arm64.tar.gz"

          sha256sum ${{ env.BINARY_NAME }} > ${{ env.BINARY_NAME }}.sha256
          tar -czf "$ARCHIVE" ${{ env.BINARY_NAME }}
          sha256sum "$ARCHIVE" > "${ARCHIVE}.sha256"

          echo "=== Distribution ==="
          ls -la

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.BINARY_NAME }}-linux-arm64
          path: |
            dist/*.tar.gz
            dist/*.tar.gz.sha256
          retention-days: 90

  upload-to-release:
    needs: [build-linux-x64, build-linux-arm64]
    runs-on: ubuntu-latest
    if: github.event.inputs.upload_to_release != ''
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: List artifacts
        run: ls -la dist/

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.upload_to_release }}
          files: dist/*
          fail_on_unmatched_files: false
