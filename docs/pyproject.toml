[project]
name = "flint-docs"
version = "0.1.0"
description = "Flint documentation — Fleet GitOps YAML linter and language server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.30",
]
